import { Grid, Typography } from "@mui/material";

const Navbar = () => {
    return (
        <Grid sx={{bgcolor: '#121212', color: '#eaeaea', p: 3}}>
            <Typography>Navbar</Typography>
        </Grid>
    );
};

export default Navbar;
